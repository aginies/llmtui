    1. Use APIs over Logs: Whenever possible, rely on the /metrics or /health endpoints of llama.cpp for status and    
        progress instead of parsing stderr.                                                                             
     2. Atomic State Updates: Ensure that filesystem changes and configuration updates are synchronized or that         
        errors in the former trigger a rollback/correction in the latter.                                               
     3. Stream Downloads: Use streaming to write downloads directly to disk instead of buffering them in memory.        
     4. Improve Process Monitoring: Use a more robust way to monitor both stdout and stderr simultaneously without      
        one blocking the other.                                                                         

 Implementation Plan
1. Refactor Log Parsing (The "Cleanup" Phase)
Files: src/tui/app/state.rs, src/tui/app/async_ops.rs
src/tui/app/state.rs:
- 
Simplify detect_load_state: Remove logic that looks for "LOADED SUCCESSFULLY", "MAIN: MODEL LOADED", etc. The API will now handle this.
- 
Simplify parse_loading_details: Remove all logic that parses metrics (TPS, VRAM, context size, etc.) from log strings.
- 
Keep Progress Parsing: Retain the logic for LoadingTensors (parsing "loading tensor X of Y") so the progress bar remains smooth.
src/tui/app/async_ops.rs:
- 
Clean poll_server_logs: Remove the massive block of code that regex-matches metrics (TPS, CPU, VRAM, etc.) from the log stream. This function should now only be responsible for adding lines to the log panel.
2. Implement API-driven Loading (The "Proactive" Phase)
Files: src/tui/app/async_ops.rs, src/tui/app/state.rs
Add /health Polling: 
- 
In the Loading state, implement a periodic check (e.g., every 500ms) to the /health endpoint.
- 
When /health returns a successful status and indicates a model is active, trigger the transition to ModelState::Loaded.
Update ModelState Transitions: Ensure that the logic previously in detect_load_state (updating model_states and loaded_model_names) is now triggered by the /health check result.
3. Robust Exit Detection (The "Process" Phase)
Files: src/backend/server.rs
Enhance Background Task: 
- 
The current background task in spawn_server waits for stdout/stderr to end. 
- 
Ensure that the child.wait() result is used to explicitly signal the App via a channel that the server has exited, rather than waiting for a log line containing "EXITED".
4. Verify Metrics Flow
Files: src/tui/app/async_ops.rs
Ensure poll_metrics is the Sole Provider: Verify that the metrics_polling_task (which uses /metrics) is the only component updating app.metrics.
🛠️ Summary of Changes by Module
Module	Current Role	New Role
state.rs	Parses logs for everything	Parses logs only for progress bar
async_ops.rs	Parses logs for metrics	Polls /health for loading completion
server.rs	Signals exit via log strings	Signals exit via process exit status
metrics_task	(Redundant/Fallback)	Primary source of truth
